# Прогнозирование активности пользователей 📈

В этом разделе рассматривается задача прогнозирования пользовательской активности для оценки нагрузки на серверы.  

---

## Описание задачи 📝

Чем активнее пользователи, тем выше нагрузка на сервера. В последнее время участились жалобы на подвисания приложения.  
Задача: спрогнозировать, как изменится активность пользователей в течение ближайшего месяца, чтобы заранее оценить нагрузку и оптимизировать работу инфраструктуры.

---

## Основная метрика 🎯

- **Метрика:** `views` (просмотры)  
- **Причина выбора:** `views` напрямую отражает активность пользователей и нагрузку на серверы. Эта метрика позволяет выявлять пики нагрузки и аномалии.  
- **Временное разрешение:** дневное (daily).  

---

## Регрессоры и особенности данных 

- Основной регрессор: **DAU** (daily active users) – опережающий индикатор активности.  
- Учет аномалий: период флешмоба **05.12.2025 – 11.12.2025**, который показал статистически значимый рост просмотров.  

> Флешмоб сильно влияет на `views`, поэтому его необходимо учитывать как отдельный фактор.

---



## Выбранные модели

1. **DLT + MCMC** – модель DLT (Dynamic Linear Trend) с оценкой параметров через Markov-Chain Monte Carlo.  
2. **DLT + MAP** – модель DLT с оценкой параметров через Maximum a Posteriori.  
3. **DLT + MCMC + регрессоры** – MCMC с учетом регрессоров DAU и аномального периода (флешмоб).  
4. **DLT + MAP + регрессоры** – MAP с учетом регрессоров DAU и аномального периода (флешмоб).  
  

---

## Ограничения ⚠️

- Прогноз на длинный горизонт менее надежен из-за ограниченного объема исторических данных.  
- Флешмобы и аномальные события сильно влияют на точность модели.  
- Необходимо контролировать корректность регрессоров (DAU), чтобы избежать систематического завышения прогноза.

---
## Вывод 

- Основная метрика `views` позволяет оценить нагрузку на серверы.  
- Флешмоб 05–11.12.2025 выявил важность учета аномальных периодов.  
- Текущие модели завышают прогноз, поэтому необходимо перепрогнозировать DAU и повторно проверить модели 3 и 4 для более точного прогноза активности пользователей.  

---
## Используемые технологии 🛠️

- Python 3.x  
- Pandas, Numpy  
- Matplotlib, Seaborn  
- Scikit-learn (регрессии, валидация)  
- Statsmodels (статистический анализ)  
- PyMC3 / PyStan (для MCMC)  

