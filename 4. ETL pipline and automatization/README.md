# ETL-pipline и автоматизация аналитики приложения 

В этом проекте реализованы:
- агрегация+выгрузка данных в кликхаус
- автоматизация базовой отчетности по активности пользователей приложения с ежедневной генерацией сводных метрик и отправкой аналитических отчетов в Telegram.

---

## Структура проекта 🗂️

Проект состоит из трёх основных скриптов:

1. **`etl_pipeline.py`** – основной ETL-пайплайн:  
   - Объединяет данные `feed_actions` и `message_actions` в одну таблицу  
   - Считает метрики по срезам: `os`, `gender`, `age`  
   - Загружает финальные агрегированные данные в ClickHouse  
   - Обеспечивает ежедневное дополнение таблицы новыми данными  

2. **`bot_feed_and_messages.py`** – объединяет данные о ленте и данные по сообщениям (`message_actions`) для каждого пользователя:  
   - Число отправленных сообщений (`messages_sent`)  
   - Число полученных сообщений (`messages_received`)  
   - Количество пользователей, которым отправляли сообщения (`users_sent`)  
   - Количество пользователей, от которых получали сообщения (`users_received`)  

3. **`bot_feed.py`** – формирует ежедневный отчет по активности пользователей в ленте (`feed_actions`):  
   - Число просмотров (`views`)  
   - Число лайков (`likes`)  
   Каждая выгрузка выполняется отдельным таском.



---

## Структура финальной таблицы 📋

| Название поля         | Описание |
|-----------------------|----------|
| `event_date`          | Дата события |
| `dimension`           | Название среза (`os`, `gender`, `age`) |
| `dimension_value`     | Значение среза |
| `views`               | Число просмотров |
| `likes`               | Число лайков |
| `messages_received`   | Число полученных сообщений |
| `messages_sent`       | Число отправленных сообщений |
| `users_received`      | Количество пользователей, от которых получены сообщения |
| `users_sent`          | Количество пользователей, которым отправлены сообщения |

> Каждое утро таблица обновляется новыми данными и автоматически попадает в ClickHouse для дальнейшего анализа.

---

## Автоматизация отчетности 📈

- Скрипт ежедневно формирует сводку по всему приложению как единому целому.  
- Генерация отчета происходит автоматически и отправляется в Telegram, обеспечивая оперативный контроль за ключевыми метриками.  

**Ключевые метрики:**
- Активность пользователей в ленте (`DAU `,`views`, `likes`, `CTR`)  
- Активность пользователей в мессенджере (`DAU`, изменение со вчера, в разрезе OS, географии, возраста)  

---

## Используемые технологии 🛠️

- Python 3.x  
- Pandas, Numpy – для обработки данных  
- ClickHouse (clickhouse-connect) – для хранения агрегированных данных  
- Telegram Bot API – для отправки ежедневных сводок  
- Airflow – для планирования ежедневного запуска ETL  

---

Задача обеспечивает:  
- Автоматический сбор и агрегацию метрик пользователей  
- Ежедневное обновление данных в ClickHouse  
- Автоматическую отправку сводной аналитики в Telegram  

Это позволяет оперативно отслеживать активность пользователей и нагрузку на приложение, выявлять аномалии и принимать решения по оптимизации работы системы.